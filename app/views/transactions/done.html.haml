.Flex.Center.Base
  .Container
    .Header
      = image_tag "material/logo/logo.png", alt: "フリマロゴ", class: 'logo'
    .Main
      .purchase-done
        .purchase-done__item-frame
          .detail-done
            .detail-done__image
              - @item.images.first(1).each do |image|
                = image_tag image, alt: "商品画像", class: 'done-item-image'
            %p.detail-done__title 購入が完了しました！
        .purchase-done__name-frame
          .title-done 
            = @item.name
        .purchase-done__cos-frame
          .cost-done
            .cost-done__price 
              = @item.freight
            .cost-done__yen ￥
            .cost-done__sum 
              = @item.price
        .purchase-done__decision-frame
          .decision-done
            = link_to root_path, class: "decision-done__btn" do
              %button.decision-done__btn トップページへ戻る
    .Footer
      = image_tag "material/logo/logo.png", alt: "フリマロゴ", class: 'logo'
      .Footer__text © FURIMA 
